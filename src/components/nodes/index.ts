import {TextNode ,OutputNode} from "@/components/nodes/settings";
import {IfNode} from "@/components/nodes/logic-nodes";

export const nodeTypes = {
    text: TextNode,
    outputext: OutputNode,
    if: IfNode,
  };